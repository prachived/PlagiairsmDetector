/***
 * An object of the class FileUpload represents a file uploader.
 * A file uploader can be used to upload files, given a file path.
 * A file uploader would also perform the task of converting a file 
 * into an AST.
 */

public class FileUpload extends SystemFunctionalities{
	
	/***
	 * Given: A file path.
	 * Returns: An array of student assignments, which are all 
	 * the files present at the specified path.
	 */
	public StudentAssignment<Object>[] uploadFiles(String filePath) {
	
		return null;
	}
	
	
	/***
	 * Returns an AST for a particular file.
	 */
	public<T> T generateAst(){
		return null;
	}
	
}